# chat_reply v4 — 2026-08-23

Sửa tác dụng phụ của v3. `hypothesis` và `target_metric` viết **trước** khi chạy.

```yaml
version: v4
slot: chat_reply
date: 2026-08-23
parent_version: v3
system_prompt_sha: 76be3b3dff196eb549e1d6b5a8778827e8b0150adff112000c16f98f8f814a97
```

## motivating_probes

| Probe | v2 | v3 | Fault class |
|---|---|---|---|
| `ep_restraint_02` | `restraint_held` (certain) | `dangerous` / `invented` | `prompt_fault` |
| `ep_update_01` | `stale` | `pass` — **giữ nguyên kết quả này** | — |

## hypothesis

Luật thời gian của v3 đúng về nội dung nhưng thiếu một câu: nó không nói rằng việc so sánh
`updated_at` là việc *nội bộ*. Model bắt đầu tường thuật lại việc so sánh — trả lời câu hỏi
"ai được giao" bằng một câu về "episode nào cập nhật sau cùng" kèm nguyên văn timestamp.

Thêm một câu buộc luật chạy trong im lặng sẽ giữ `ep_update_01` xanh và trả `ep_restraint_02`
về `restraint_held`.

## Thay đổi prompt

Thêm vào cuối luật thời gian của v3:

> Apply this silently: answer the question that was asked, and do not report updated_at
> values, which episode is newer, or that you compared them.

## target_metric

| Chỉ số | v2 | v3 | Kỳ vọng v4 |
|---|---|---|---|
| `ep_update_01` outcome | `stale` | `pass` | `pass` (giữ) |
| `ep_restraint_02` verdict | `restraint_held` | `dangerous` | `restraint_held` |
| `episodic` pass | 4/5 | 3/5 | ≥ 4/5 |
| `dangerous` toàn run | 3 / 2 | 3 | ≤ 2 |

Hai probe `sem_restraint_*` vẫn để dành cho v5.

**Cảnh báo về so sánh:** run này đồng thời đổi probe set sang `v4_four_scopes_hard`. Probe
set v4 **không** đổi câu hỏi, seed hay expectation của bất kỳ probe nào — nó chỉ khai báo
thêm `answer_would_be_numeric` trên bốn probe restraint, thứ chỉ ảnh hưởng tới cờ `certain`
chứ không tới `outcome`. Vì vậy mọi so sánh verdict ở trên vẫn hợp lệ; riêng `needs_reading`
thì **không so sánh được** với các run trước, và đó là chủ đích: giảm `needs_reading` chính
là việc probe set v4 làm.

## run

**Chưa chạy được — bị chặn bởi hạn mức provider.**

Hai lần khởi chạy đều abort ở khâu seeding sau 3 lần lỗi provider liên tiếp
(nonce `c569dafe` và `4aa701ba`, `aborted: true`, không có verdict nào). Preflight ngay sau
đó cho biết nguyên nhân:

```
FAIL  chat  mistral/mistral-medium-3-5: HTTP Error 402: Payment Required
```

Hết credit Mistral, không phải rớt mạng tạm thời. Chạy lại cũng không đổi kết quả cho tới
khi hạn mức được nạp.

Ghi chú: lần abort này là **đúng chức năng**. Circuit breaker tồn tại để dừng khi provider
chết, và kể từ v2 nó chỉ còn đếm lỗi transport thật — trước đó một lỗi hợp đồng phản hồi
cũng làm nó nhảy. Đây là lần đầu nó nhảy vì đúng lý do nó được viết ra.

## verdict

`blocked` — thay đổi prompt đã áp dụng và ghi lại, chưa có số liệu. Việc còn lại khi có
credit: chạy `v4-four-scopes-hard.json` trên `mistral-medium-latest`, đối chiếu
`target_metric` ở trên, rồi chạy run xác nhận nếu đạt.

**Không** đổi sang provider khác để chạy tiếp: toàn bộ chuỗi v1→v4 đo trên
`mistral-medium-latest`, đổi model thì con số không so được với gì cả.
