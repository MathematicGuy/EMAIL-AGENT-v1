# chat_reply v4 on mimo-v2.5-pro — 2026-08-23

**Không so sánh được với chuỗi mistral v1→v4.** Cùng `system_prompt_sha` với
`v4-2026-08-23.md`, model khác. Ledger mistral v4 vẫn `blocked`; file này là series riêng.

Cùng-model prior: `evaluations/MEMORIES/baselines/mimo-v2.5-pro-v3-parallel.json`
(nonce `152acd31`, cùng SHA, probe set **v3**). Verdict/outcome so được; `needs_reading` thì
không — probe set v4 chỉ thêm `answer_would_be_numeric` trên bốn probe restraint.

```yaml
version: v4
slot: chat_reply
date: 2026-08-23
parent_version: v4
model: mimo-v2.5-pro
provider: mimo
system_prompt_sha: 76be3b3dff196eb549e1d6b5a8778827e8b0150adff112000c16f98f8f814a97
probe_set: v4_four_scopes_hard
```

## hypothesis

Prompt v4 đã nằm trên disk. Run này đo nó trên `mimo-v2.5-pro` với probe set v4, không
đổi chữ prompt. Kỳ vọng: giữ các outcome của prior mimo v3-parallel, và `needs_reading`
giảm nhờ bốn cờ numeric.

## target_metric

| Chỉ số | mimo v3-parallel (v4 prompt, v3 probes) | Kỳ vọng run này (v4 probes) |
|---|---|---|
| `ep_update_01` | `scope_earned_it` / pass | giữ |
| `ep_restraint_02` | `restraint_held` | giữ |
| `ep_recall_01` | `scope_earned_it` | giữ (mimo đã earn; không dùng số mistral) |
| full-arm pass | 17/20 | ≥ 17/20 |
| `dangerous` | 3 | ≤ 3 |
| `needs_reading` | 8 | giảm |
| seed_failures / aborted | 0 / false | 0 / false |

## run

Chưa chạy.

## verdict

Chưa có số liệu.
