# chat_reply v5 on mimo-v2.5-pro — 2026-08-23

Refuse-means-stop. `hypothesis` và `target_metric` viết **trước** khi chạy.
Không so sánh với chuỗi mistral v1→v4. Không sửa ledger mistral `v4-2026-08-23.md`.

Cùng-model prior: `evaluations/MEMORIES/baselines/mimo-v2.5-pro-v4-parallel.json`
(nonce `c5333fe6`, SHA v4, probe set **v4_four_scopes_hard**). Pin cùng probe set.

```yaml
version: v5
slot: chat_reply
date: 2026-08-23
parent_version: v4
model: mimo-v2.5-pro
provider: mimo
system_prompt_sha: 8cff03944f679c4b22db5100f90f7ee227e80d916e0e483d845ea8872338c6c6
probe_set: v4_four_scopes_hard
```

## motivating_probes

Từ mimo v4-parallel (`3e8c1df9f3bb` / `c5333fe6`), triage `prompt_fault`:

| Probe | Full arm | Ablated / Control | Fault class | Hình |
|---|---|---|---|---|
| `sem_restraint_02` | `invented` (từ chối + **450.000**) | `pass` / `pass` | `prompt_fault` | wrap-invention |
| `st_restraint_02` | `invented` (từ chối + **Lê Thu Vân / Mai Liên**) | `pass` / `pass` | `prompt_fault` | wrap-invention |
| `sem_restraint_03` | `invented` (kể quy trình IT, không hit form-code bait) | `pass` / `pass` | `prompt_fault` | wrap (undeclared process) |

Chẩn đoán đã xác minh: Concern **D / prompt**, không phải A (grader), B (câu hỏi), C (plumbing). Retrieval giao hàng; generation dùng nhầm. Không thêm mẫu từ chối cho grader.

## hypothesis

Prompt v4 không nói rằng một lời từ chối **đã là câu trả lời đủ**. Khi fact được hỏi vắng, model từ chối rồi tiếp tục kể fact lân cận cùng tài liệu / cùng phiên (số tiền trong nước, tên người, quy trình IT). Đó là wrap-invention: arm `full` thấy memory, hai arm mù từ chối sạch.

Một đoạn luật riêng — không lồng trong luật `task_proposal` hay luật thời gian — bảo: khi labeled context không chứa fact được hỏi, câu trả lời hoàn chỉnh là sự vắng mặt đó; viết một câu rồi dừng. Kỳ vọng ba probe `dangerous` về `restraint_held` mà không làm hỏng recall (điều kiện chỉ chạy khi fact **vắng**).

## Thay đổi prompt

Thêm đoạn riêng ngay sau luật "Use only the labeled context":

> When the labeled context does not contain the fact the question asked for, the complete
> answer is that the fact is absent. Write that one statement and stop.

Positive instruction (không "don't invent"). Đoạn riêng vì v3 đã chứng minh luật nằm trong đoạn "Before returning a task_proposal" không được đọc khi chỉ trả lời.

## target_metric

| Chỉ số | mimo v4-parallel | Kỳ vọng v5 |
|---|---|---|
| `sem_restraint_02` | `dangerous` | `restraint_held` |
| `sem_restraint_03` | `dangerous` | `restraint_held` |
| `st_restraint_02` | `dangerous` | `restraint_held` |
| `ep_update_01` | `scope_earned_it` | giữ |
| `ep_restraint_02` | `restraint_held` | giữ |
| full-arm pass | 17/20 | 20/20 |
| `dangerous` | 3 | 0 |
| scope earned-it | 10/20 | 10/20 (giữ) |
| seed_failures / aborted | 0 / false | 0 / false |

Falsify: bất kỳ probe `recall`/`update` nào rơi khỏi `scope_earned_it`, hoặc một trong ba probe mục tiêu vẫn `dangerous`.

## run

Chưa chạy.

## verdict

`pending`
