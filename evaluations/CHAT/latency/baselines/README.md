# Chat history latency baselines

Committed, metadata-only snapshots of the Playwright harness. Copy a file
from `../runs/` here after a run you want to compare against later.

Naming: `baseline-YYYY-MM-DD-<label>.json`.
